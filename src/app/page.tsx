import { Button } from "@/components/ui/button";

export default function Home() {
  return (
    <div className="text-red-500">
      <h1>Hello World</h1>
      <Button variant="custom">Click meeee</Button>  {/* so int hsi verient we can use default provided design also and make custom aslo go to button and in varient  we add custom tailwind */}
    </div>
  );
}
